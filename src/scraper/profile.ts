import { ProfileData } from "../types";

export async function scrapeProfile(userId: string): Promise<ProfileData> {
  // TODO: Implement in Phase 2
  throw new Error(`Profile scraping not yet implemented for ${userId}`);
}
