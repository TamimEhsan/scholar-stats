import { PaperData } from "../types";

export async function scrapePaper(
  userId: string,
  paperId: string
): Promise<PaperData> {
  // TODO: Implement in Phase 2
  throw new Error(`Paper scraping not yet implemented for ${userId}:${paperId}`);
}
